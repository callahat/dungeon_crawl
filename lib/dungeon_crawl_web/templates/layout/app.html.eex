<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Hello DungeonCrawl!</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="container">
      <header class="header">
        <nav role="navigation">
          <ul class="breadcrumb text-right">
            <li class="pull-left"><%= link "Dungeon Crawl", to: Routes.crawler_path(@conn, :show) %></li>
            <%= if @current_user do %>
              <li><%= link @current_user.username, to: Routes.user_path(@conn, :show) %></li>
              <li><%= link "Edit Dungeons", to: Routes.dungeon_path(@conn, :index) %></li>
              <li><%= link "Tile Templates", to: Routes.manage_tile_template_path(@conn, :index) %></li>
              <li><%= link "Log out", to: Routes.session_path(@conn, :delete, @current_user), method: "delete" %></li>
              <%= if @current_user.is_admin do %>
              <li><%= link "Users", to: Routes.manage_user_path(@conn, :index) %></li>
              <li><%= link "Dungeons", to: Routes.manage_dungeon_path(@conn, :index) %></li>
              <% end %>
            <% else %>
              <li><%= link "Register", to: Routes.user_path(@conn, :new) %></li>
              <li><%= link "Log in", to: Routes.session_path(@conn, :new) %></li>
            <% end %>
          </ul>
        </nav>
        <!-- <span class="logo"></span> /-->
      </header>

      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
    <script>window.userToken = "<%= assigns[:user_token] %>"</script>
    <script src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
