<h2>Listing users</h2>

<table class="table">
  <thead>
    <tr>
      <th>Id</th>
      <th>First Name</th>
      <th>Username</th>
      <th>Admin?</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for user <- @users do %>
    <tr>
      <td><%= user.id %></td>
      <td><%= first_name user %></td>
      <td><%= user.username %></td>
      <td><%= user.is_admin %></td>

      <td class="text-right">
        <%= link "Show", to: Routes.manage_user_path(@conn, :show, user), class: "btn btn-default btn-xs" %>
        <%= link "Edit", to: Routes.manage_user_path(@conn, :edit, user), class: "btn btn-default btn-xs" %>
        <%= link "Delete", to: Routes.manage_user_path(@conn, :delete, user), method: :delete, data: [confirm: "Are you sure?"], class: "btn btn-danger btn-xs" %>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<%= link "New user", to: Routes.manage_user_path(@conn, :new) %>
